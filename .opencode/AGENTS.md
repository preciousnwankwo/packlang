# Packlang Agent Instructions

You are building a programming language called **pack** in C.

## Build

```bash
make
./packc examples/hello.pack
```

## Approach

Ghuloum's Incremental Approach — one feature per commit, compiler always works.

## Git

- Commits authored as `packagent <preciousnwankwo+packagent@users.noreply.github.com>` (already in git config)
- `git add -A && git commit` with sensible message

## Skills

Load and use skills from `.agents/skills/` when relevant.
